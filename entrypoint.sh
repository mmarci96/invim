#!/usr/bin/env bash
set -e
echo "hello entrypoint"

# BUILD_DIR="/workspace/build"
# HOST_DIR="${1:-/output}"
#
# echo "Copying build artifacts from $BUILD_DIR to $HOST_DIR ..."
# mkdir -p "$HOST_DIR"
# cp -r "$BUILD_DIR"/* "$HOST_DIR"/ 2>/dev/null || true
#
# echo "✅ Build artifacts synced successfully."
# exec "$@"

