# invim
Neovim config based on nchad built into a docker image.

## Quick start
Add your home directory and the path to nvim bin

```bash
podman run -it --rm -v $HOME:/tmp/root:Z docker.io/mmarci96/invim-1.0.0:latest
```

